# FastAPI wrapper deps only — chatterbox-tts/torch/perth are assumed already
# installed in the existing Python environment (per the integration spec).
fastapi
uvicorn[standard]
pydantic
soundfile
psutil
httpx
pytest
# Fast tier (facebook/mms-tts-ara / -eng): plain torch (already present for
# Chatterbox) + transformers for VitsModel/AutoTokenizer.
transformers
